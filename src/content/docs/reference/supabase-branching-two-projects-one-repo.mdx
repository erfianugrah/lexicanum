---
title: "Two Supabase projects on one GitHub repository"
description: A monorepo with two apps, each with its own supabase/ directory and its own Supabase project, both connected to one repository through the GitHub integration. Which pull requests create a preview on which project with "Supabase changes only" off and on, what the Supabase Preview checks on a shared commit look like, why the docs' wait-by-check-name workflow reads the other project's result, and a per-project signal that does not.
author: Erfi Anugrah
category: supabase
group: operations
---

import { Aside } from "@astrojs/starlight/components";

A monorepo holds two apps, `apps/a/` and `apps/b/`, each with its own
`supabase/` directory and its own Supabase project. Both projects connect to
the same GitHub repository through the Supabase GitHub integration, each with
its own working directory, and both have Automatic branching on. Two
questions follow: which pull requests create a preview branch on which
project, and whether a CI job can wait for one project's preview when both
report checks on the same commit.

Every figure marked measured comes from
[experiments/github-branching](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching)
in the [supabase-lab](https://github.com/erfianugrah/supabase-lab) harness, run
on 2026-09-23 against two Micro projects in `ap-southeast-1` on a Pro
organization and a private throwaway repository, all created and destroyed the
same day. Project A was connected with Working directory `apps/a`, project B
with `apps/b`. The branching docs the results are compared against were read
the same day.[^sb-gh-integration][^sb-working-branches] Anything not marked
measured is either cited or labelled as a design or as not run.

**TL;DR:**

- Connecting a second project to a repository another project already uses is
  accepted (B's first attempt did not save; it did on reconnect); each
  connection keeps its own working directory.
- With "Supabase changes only" off, each project created a preview for all 5
  pull requests in the run, a root `README.md` change included. With it on, a
  project created a preview only for pull requests that changed files under
  its own `<workdir>/supabase/` (tested with migrations).
- Every connected project posted check-runs named `Supabase Preview`, from
  the same GitHub App, to every pull request head commit read.
  Every project posts at least one `skipped` run; a project that does not
  branch posts only `skipped` runs, 2 of them.
- The docs' wait-by-check-name workflow, path-filtered to app A, returned
  project B's `skipped` on an A-only pull request, so its migrate job was
  skipped, and B's `success` on a pull request touching both apps.
- Per project, the Management API branch list reports the preview's state:
  `MIGRATIONS_FAILED` for A and `FUNCTIONS_DEPLOYED` for B on the same pull
  request. Its first reading, at 15 s, was `FUNCTIONS_DEPLOYED` on both,
  before `CREATING_PROJECT` appeared at 32 s.

```dot
digraph {
  rankdir=LR; bgcolor="transparent"; nodesep=0.3; ranksep=0.7;
  fontname="Helvetica,Arial,sans-serif";
  node [fontname="Helvetica,Arial,sans-serif", fontsize=11, shape=box];
  edge [fontname="Helvetica,Arial,sans-serif", fontsize=9];

  repo [label="GitHub repository\napps/a/supabase/\napps/b/supabase/"];
  pr [label="pull request\nhead commit"];
  a [label="project A\nworking directory apps/a"];
  b [label="project B\nworking directory apps/b"];
  pa [label="preview branch A"];
  pb [label="preview branch B"];
  checks [label="check-runs on the commit\nall named Supabase Preview"];

  repo -> pr;
  pr -> a [label="GitHub App"];
  pr -> b [label="GitHub App"];
  a -> pa [label="if it branches"];
  b -> pb [label="if it branches"];
  a -> checks [label="2 to 6 runs"];
  b -> checks [label="2 to 6 runs"];
}
```

1. One pull request's head commit reaches both projects through the one GitHub App.
2. Each project decides on its own whether to create a preview branch.
3. Both projects post their check-runs to the same commit under the same name,
   whether they branch or not.

## Which setup to pick

| Goal | Setup | Status |
|---|---|---|
| A pull request that touches only app A creates only A's preview | "Supabase changes only" on, on both projects | measured |
| Every pull request gets a preview on both projects | "Supabase changes only" off | measured; 2 previews per pull request, each billed for its own usage[^sb-branching-usage] |
| A CI job waits for app A's preview before seeding or testing it | poll project A's branch list for the pull request's git branch | measured signal; the wait loop below is a design, not run in CI |
| A CI job waits on the `Supabase Preview` check by name | do not use with two projects connected | measured: it read the other project's run |
| Try to have only one project report on a commit | Automatic branching off on one project; create its branches from CI | not run; whether a connected project with Automatic branching off still posts check-runs is untested |

## Preview creation: the working directory and "Supabase changes only"

The docs define the working directory as "the path from your repository root
to the directory that contains the `supabase/` folder", and the setting as
"only create Supabase branches when Supabase files change".[^sb-gh-integration]
Neither says which files count when two projects share a repository. The run
opened the same five pull requests once with the setting off on both projects
and once with it on.

| Pull request changes | Off: preview on A | Off: preview on B | On: preview on A | On: preview on B |
|---|---|---|---|---|
| `README.md` at the repository root | yes | yes | no | no |
| `apps/a/README.md` | yes | yes | no | no |
| a migration under `apps/a/supabase/` | yes | yes | yes | no |
| a migration under `apps/b/supabase/` | yes | yes | no | yes |
| a migration under each | yes | yes | yes | yes |

- Off, the working directory did not limit which pull requests previewed.
  Each project created 5 previews for 5 pull requests, 10 in all.
- On, each project previewed exactly the pull requests that changed a
  migration under its own `<workdir>/supabase/` (5 shapes, one pull request
  each). `apps/a/README.md` sits inside A's working directory and did not
  trigger A.
  The files changed under `supabase/` were migrations only; `config.toml`,
  `seed.sql` and functions were not tested.
- Closing the pull requests removed every preview within 2 minutes in both
  runs; nothing was left for the harness to delete.
- The connection form set Branch limit to 3; GB00 read `limit=3` back on
  both projects. The run raised it to 10 on both first, so the limit could
  not hide a preview.

## The checks on a shared commit

Each project reports its branching pipeline to GitHub as check-runs. With two
projects connected, all of them land on the same commit.

| On the head commit | Measured |
|---|---|
| Check-run name | `Supabase Preview` on every run, from both projects |
| GitHub App and check suite | `supabase` for every `Supabase Preview` run; one check suite on the setting-off commits (RUNLOG snapshot). Commits with the wait workflow also carried that workflow's own `github-actions` runs |
| Runs from a project that branched | 5, once 6: 1 `skipped`, 1 `success` (or `failure`), and 3 (once 4) still `in_progress` at the end of the observation window (600 s; 480 s for the failure run; setting-off run: snapshot at 01:59:12 UTC, inside the window) |
| Runs from a project that did not branch ("Supabase changes only" on) | 2, both `skipped` |
| Field that identifies the project | `details_url` only (checked: `external_id`, `details_url`, output title, output summary). It links the parent project's dashboard on the early runs and the preview branch's own project on later ones |
| `external_id` | empty on every run |
| Output title | `Supabase Preview` or empty |

GitHub's check-runs endpoint defaults to `filter=latest`, "the most recent
check runs".[^gh-check-runs] In the setting-on scoping run, that view
returned a single `Supabase Preview` run. On 2 of the 5 pull requests (the
A-only and the two-app one) the project it belonged to changed during the
build: on the A-only pull request, sampled every 3 s for the first 150 s, the
name-filtered default view showed two different B `skipped` runs in turn
before A's first `in_progress`, then A's `success`. Pass `filter=all` to see every run.

## Waiting for one project's preview in CI

The branching docs show a CI job that waits for the preview with
`fountainhead/action-wait-for-check@v1.2.0` and `checkName: Supabase
Preview`, then gates a migrate job on the result being
`success`.[^sb-working-branches] The action lists check-runs by name with the
endpoint's default filter and returns the conclusion of the first completed
one.[^wait-action-poll] The run added that workflow to the repository with a
path filter of `apps/a/supabase/**`, the migrate job reduced to an echo, and
"Supabase changes only" on for both projects.

| Pull request | Wait step returned | That run belonged to | Migrate job | A's own first `success` (first seen, 30 s poll) |
|---|---|---|---|---|
| migration under `apps/a` only | `skipped` | project B | skipped | 108 s after opening |
| migration under each app | `success` | project B | ran | 108 s after opening |

On the A-only pull request, the B `skipped` run the wait step read started
at 02:22:13 UTC. A's own `skipped` run had completed at 02:22:10 but was not
the latest; A's `success` run started at 02:22:58 and was first seen
completed by the 108 s poll. The path filter decides
which pull requests run app A's workflow (no B-only pull request was opened in
this run); it cannot stop B's runs being on A's commits, and with "Supabase
changes only" on, B posts `skipped` runs to every pull request commit it does
not branch for.

The Management API's branch list is per project, so it reports each
project's preview separately. `GET /v1/projects/{ref}/branches` on the parent project, matched on
`git_branch`, was sampled on one pull request that broke A's migration and kept B's
valid:

| First sample showing the value (s after opening, 15 s polling) | A branch `status` | B branch `status` |
|---|---|---|
| 15 | `FUNCTIONS_DEPLOYED` | `FUNCTIONS_DEPLOYED` |
| 32 | `CREATING_PROJECT` | `CREATING_PROJECT` |
| 65 | `RUNNING_MIGRATIONS` | `RUNNING_MIGRATIONS` |
| 81 | `MIGRATIONS_FAILED` | `FUNCTIONS_DEPLOYED` |

Both branch rows read `FUNCTIONS_DEPLOYED` at 15 s, before `CREATING_PROJECT`
appeared at 32 s, so a loop that exits on that value can return before the
preview project is created. The published OpenAPI document marks `status` as
deprecated, with "List action runs to get branch status instead".[^sb-openapi]
The preview project's latest action run
(`GET /v1/projects/{preview_ref}/actions`) showed `migrate` `DEAD`, with
`deploy` and `seed` still `CREATED`, on A at 81 s, and every step `EXITED` on
B.

In the wait-workflow run, the action runs that carried a `check_run_id` and
pointed at that run's commits were 3, one per preview project (1 on the
A-only pull request, 2 on the two-app one). Each pointed at a run left
`in_progress` at the end of the 600 s window while the action run's steps
were all `EXITED`, so it cannot serve as a wait target. The parent projects'
action runs pointed at no run on those commits.

A wait loop built on the branch list, as a design (the calls are the ones the
run made from the harness; the loop was not run in CI):

```bash
# Needs SUPABASE_ACCESS_TOKEN and this app's parent project ref.
seen_busy=0
for i in $(seq 1 60); do
  s=$(curl -s -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
        "https://api.supabase.com/v1/projects/$PROJECT_REF/branches" |
      jq -r --arg b "$GITHUB_HEAD_REF" '.[] | select(.git_branch == $b) | .status')
  case "$s" in
    CREATING_PROJECT|RUNNING_MIGRATIONS|MIGRATIONS_PASSED) seen_busy=1 ;;
    MIGRATIONS_FAILED|FUNCTIONS_FAILED) [ "$seen_busy" = 1 ] && exit 1 ;;
    FUNCTIONS_DEPLOYED) [ "$seen_busy" = 1 ] && exit 0 ;;
  esac
  sleep 10
done
exit 1
```

`MIGRATIONS_PASSED` and `FUNCTIONS_FAILED` come from the OpenAPI enum and
were not observed in the run.[^sb-openapi] Requiring a non-terminal state
first covers the first reading, which was `FUNCTIONS_DEPLOYED` at 15 s on
both rows before `CREATING_PROJECT`. It also means a loop that starts after
the preview has finished never sees one and times out; a job that can start
late could compare the branch's `updated_at` with the head commit's time
instead (`updated_at` was not read in the run; untested).

<Aside type="caution" title="One failure sample">
The failure path rests on one run with one kind of failure, a migration that
errors. Seed, configuration and function-deploy failures were not run, and
neither was a check of whether a `DEAD` step always means a failed one.
</Aside>

## What to do about it

| Practice | Evidence | Module |
|---|---|---|
| Turn "Supabase changes only" on for every project sharing the repository. | Off, each project previewed all 5 pull requests; on, only those changing its own `<workdir>/supabase/`. | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) |
| Keep files that should not trigger a preview outside `<workdir>/supabase/`. | `apps/a/README.md` inside A's working directory did not trigger A with the setting on. | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) |
| Raise Branch limit before relying on per-pull-request previews. | The connection form set it to 3; GB00 read `limit=3` back on both projects. | [GB00](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb00-control.ts) |
| Do not wait on the `Supabase Preview` check by name with two projects connected. | The docs' workflow for app A read B's `skipped` on an A-only pull request and B's `success` on a two-app one: 2 of 2 pull requests, one run each. | [GB02](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb02-wait-by-name.ts) |
| Read check-runs with `filter=all` when inspecting a shared commit. | The default view returned one run per name; a commit where both projects branched carried 10 or 11 `Supabase Preview` runs (10 on the five setting-off commits, per the RUNLOG snapshot; 11 on the setting-on two-app commit). | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) |
| Wait on your project's branch list, matched on `git_branch`, once a non-terminal status is seen. | `status` separated `MIGRATIONS_FAILED` from `FUNCTIONS_DEPLOYED` per project; the first reading, at 15 s, was `FUNCTIONS_DEPLOYED` before `CREATING_PROJECT` appeared. | [GB03](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb03-per-project-signal.ts) |
| Do not wait on an action run's `check_run_id`. | It pointed at a run left `in_progress` while the action's steps had all finished. | [GB02](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb02-wait-by-name.ts) |
| Budget for one billed preview per project per matching pull request. | Two projects previewing one pull request is two branches, each billed for its own usage;[^sb-branching-usage] with the setting off, 10 previews came from 5 pull requests. | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) |

## Where the docs are silent

| Docs | Runtime measured |
|---|---|
| no mention of connecting more than one project to one repository | two projects connected with distinct working directories, both branching |
| "Supabase changes only" matches "Supabase files", directory unstated | each project previewed the pull requests changing a migration under its own `<workdir>/supabase/` |
| the wait examples filter on `supabase/**` and wait on `Supabase Preview` by name | with two projects, the wait read the other project's run |

None of these has been filed with Supabase as of 2026-09-23.

## Reading the numbers

- The run used two projects in one organization and one region, on one day.
  The counts per commit (5 runs from a project that branched, 2 from one that
  did not; B posted 6 on the two-app pull request in the setting-on scoping
  run, 5 in the wait-workflow and failure runs) could change without notice.
  Two things held on every commit read: every run was named
  `Supabase Preview`, and every connected project posted to it.
- Preview first-seen times in the scoping runs are 30 s poll samples;
  creation latency was not measured.
- The run with the setting off used an earlier revision of the scoping module
  that read only the default check-runs view. Its check-run figures come from
  a `filter=all` snapshot taken during that run and recorded in the RUNLOG,
  not from its published artifact.
- The wait-by-name result depends on when the action's first poll lands
  relative to the check-runs. The run recorded one pull request per shape on
  GitHub-hosted runners; a different start time can pick a different
  project's run.
- A single project on its own was not tested. In the setting-on scoping
  run's A-only pull request, A's own `skipped` run came 29 s before its next
  run, and in that window the name-filtered view showed only B's `skipped`
  runs; whether a wait step ever reads a lone project's `skipped` was not
  separated.
- The Vercel integration was not part of the run.

## Evidence, by module

Measured claims map to module ids in
[experiments/github-branching](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching);
the run record is its
[RUNLOG.md](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/RUNLOG.md),
and the redacted artifacts with their measurement tables are under
[out/2026-09-23/](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching/out/2026-09-23).

| Claim | Status | How it was checked |
|---|---|---|
| Two projects connected to one repository with working directories `apps/a` and `apps/b`; Branch limit 3 on both as connected | measured | [GB00](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb00-control.ts) - connection settings read back from `GET /v2/organizations/{slug}/integrations/github/connections`, in the published v2 OpenAPI document[^sb-openapi-v2] |
| Setting off: 5 of 5 pull requests previewed on both projects | measured | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) - five pull requests opened together, both branch lists polled every 30 s for 600 s |
| Setting on: previews only for changes under the project's `<workdir>/supabase/` | measured | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) - same five shapes, second run |
| Runs per project on a commit, `skipped` runs from a project that did not branch, `details_url` as the only identifying field | measured | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) - check-runs read with `filter=all`, attributed by the refs in `details_url` |
| Default view showed B's `skipped` before A's first `in_progress` on an A-only pull request | measured | [GB01](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) - name-filtered default view sampled every 3 s for 150 s |
| Docs workflow for app A returned B's `skipped` and B's `success` | measured | [GB02](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb02-wait-by-name.ts) - the docs' workflow in the repository, the check-run id read from the action's log |
| An action run's `check_run_id` points at a run left `in_progress` | measured | [GB02](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb02-wait-by-name.ts) - action runs on each preview project matched against the commit's check-runs |
| Branch `status` per project on a failing and a passing preview; `FUNCTIONS_DEPLOYED` at 15 s before `CREATING_PROJECT` | measured | [GB03](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb03-per-project-signal.ts) - branch list and action runs sampled every 15 s for 480 s |
| The wait loop above | design | not run in CI |
| Automatic branching off on one project, its branches created from CI | not run | not tested whether such a project still posts check-runs |

## Modules

| Module | Experiment | Test | Artifact |
|---|---|---|---|
| GB00 | github-branching | [`gb00-control.ts`](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb00-control.ts) | [`out/2026-09-23`](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching/out/2026-09-23) |
| GB01 | github-branching | [`gb01-pr-scoping.ts`](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb01-pr-scoping.ts) | [`out/2026-09-23`](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching/out/2026-09-23) |
| GB02 | github-branching | [`gb02-wait-by-name.ts`](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb02-wait-by-name.ts) | [`out/2026-09-23`](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching/out/2026-09-23) |
| GB03 | github-branching | [`gb03-per-project-signal.ts`](https://github.com/erfianugrah/supabase-lab/blob/f836203/experiments/github-branching/tests/gb03-per-project-signal.ts) | [`out/2026-09-23`](https://github.com/erfianugrah/supabase-lab/tree/f836203/experiments/github-branching/out/2026-09-23) |

## Related docs

- [Supabase preview-branch compute sizing](/guides/supabase-preview-branch-compute/) - what each preview costs and how to size it, including creating branches from CI.
- [Detach a Supabase branch from its git branch](/guides/supabase-branch-detach-git-link/) - stopping a persistent branch's redeploys without deleting it.

[^sb-gh-integration]: Supabase, "GitHub integration," Supabase Docs. https://supabase.com/docs/guides/deployment/branching/github-integration
[^sb-working-branches]: Supabase, "Working with branches," Supabase Docs. https://supabase.com/docs/guides/deployment/branching/working-with-branches
[^gh-check-runs]: GitHub, "REST API endpoints for check runs," GitHub Docs. https://docs.github.com/en/rest/checks/runs#list-check-runs-for-a-git-reference
[^wait-action-poll]: fountainhead, "src/poll.ts at v1.2.0," action-wait-for-check. https://github.com/fountainhead/action-wait-for-check/blob/v1.2.0/src/poll.ts
[^sb-openapi]: Supabase, "Management API OpenAPI document (BranchResponse_Output.status)," api.supabase.com. https://api.supabase.com/api/v1-json
[^sb-openapi-v2]: Supabase, "Management API v2 OpenAPI document," api.supabase.com. https://api.supabase.com/api/v2-json
[^sb-branching-usage]: Supabase, "Manage Branching usage," Supabase Docs. https://supabase.com/docs/guides/platform/manage-your-usage/branching
